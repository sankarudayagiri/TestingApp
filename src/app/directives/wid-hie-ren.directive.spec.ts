import { WidHieRenDirective } from './wid-hie-ren.directive';

describe('WidHieRenDirective', () => {
  it('should create an instance', () => {
    const directive = new WidHieRenDirective();
    expect(directive).toBeTruthy();
  });
});
