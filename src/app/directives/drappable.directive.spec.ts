import { DrappableDirective } from './drappable.directive';

describe('DrappableDirective', () => {
  it('should create an instance', () => {
    const directive = new DrappableDirective();
    expect(directive).toBeTruthy();
  });
});
